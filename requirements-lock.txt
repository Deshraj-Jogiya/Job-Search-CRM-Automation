# Exact, fully-pinned versions of every package this app actually needs
# (including transitive dependencies), captured from a real working
# installation and verified against the full test suite (50/50 passing)
# plus a live run against the real deployment.
#
# requirements.txt stays the loose, human-editable spec (>= floors) --
# good for forking/showcase use where exact pins would be unnecessarily
# restrictive. This file is for REPRODUCIBLE installs: `pip install -r
# requirements-lock.txt` gets you the exact set that's actually been
# tested, not whatever a fresh `pip install -r requirements.txt` resolves
# to on a given day. That distinction is real, not theoretical -- a
# clean resolve against requirements.txt's current floors pulled major-
# version-bumped anthropic (0.122.0 -> 1.0.0) and openai (2.43.0 -> 3.3.1)
# packages with no guarantee anthropic_provider.py/openai's usage here
# is compatible with either.
#
# Regenerate after verifying a real dependency update against the test
# suite (and ideally a live smoke test), not automatically:
#   pip freeze > requirements-lock.txt
# then review the diff before committing it.
#
# Generated 2026-08-23 from the actual working venv, after removing two
# genuinely orphaned packages found during the Phase 21 dependency audit
# (pypdf, python-docx -- neither imported anywhere in app/, neither a
# dependency of anything else installed) and the one-off pip-audit tool
# used for that same audit (not a runtime dependency of this app).

alembic==1.19.1
bcrypt==5.0.0
annotated-doc==0.0.4
annotated-types==0.7.0
anthropic==0.122.0
anyio==4.14.0
APScheduler==3.11.2
certifi==2026.6.17
cffi==2.1.1
charset-normalizer==3.4.7
click==8.4.1
colorama==0.4.6
cryptography==50.0.0
defusedxml==0.7.1
distro==1.9.0
dnspython==2.8.0
docstring_parser==0.18.0
fastapi==0.137.2
greenlet==3.5.2
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
Jinja2==3.1.6
jiter==0.15.0
Mako==1.4.1
MarkupSafe==3.0.3
openai==2.43.0
packaging==26.3
pillow==12.3.0
playwright==1.60.0
pluggy==1.6.0
psycopg2-binary==2.9.12
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
pyee==13.0.1
Pygments==2.21.0
pytest==9.1.1
python-dotenv==1.2.2
python-multipart==0.0.32
reportlab==5.0.0
requests==2.34.2
sniffio==1.3.1
SQLAlchemy==2.0.51
starlette==1.3.1
tqdm==4.68.3
typing-inspection==0.4.2
typing_extensions==4.15.0
tzdata==2026.2
tzlocal==5.4.3
urllib3==2.7.0
uvicorn==0.49.0
