fastapi>=0.100.0
uvicorn>=0.22.0
sqlalchemy>=2.0.0
jinja2>=3.1.0
pydantic>=2.0.0
requests>=2.31.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
cryptography>=42.0.0
apscheduler>=3.10.0
dnspython>=2.6.0
psycopg2-binary>=2.9.0
playwright>=1.45.0
reportlab>=4.0.0
alembic>=1.13.0
bcrypt>=4.0.0
defusedxml>=0.7.1

# app/ingest/ -- parses the USCIS/DOL CSV+XLSX files the user manually
# drops in; openpyxl is pandas' engine for reading .xlsx
pandas>=2.2.0
openpyxl>=3.1.0

# app/services/sources/weworkremotely_source.py, jobspresso_source.py --
# RSS feed parsing for the 2 Phase 2b remote-board sources that have no
# JSON API
feedparser>=6.0.0

# LLM providers -- only the one(s) matching your LLM_PROVIDER choice are
# actually required at runtime, but listing all three keeps forking simple
anthropic>=0.34.0
openai>=1.0.0

# Testing
pytest>=8.0.0
